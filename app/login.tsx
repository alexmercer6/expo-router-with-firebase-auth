import { View, Text, Button } from 'react-native';
import React from 'react';
import { router } from 'expo-router';
import { Login } from '@/components/auth';

const LoginScreen = () => {
  return <Login title="Placeholder Login Title" />;
};

export default LoginScreen;
